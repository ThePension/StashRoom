* When creating a branch from cli, the UI is not updated (no watching mechanisms)
* If the name of a file is too long (path + name), the UI is a bit fucked
* When the editor is "unwraped", weird shits are happening
* What happens if we stages some hunks of a file, and then discard the whole file ? Even what has been staged is discarded -> Is this behaviour acceptable ?
* When switching from history mode to changes mode, the diff panel should be reset
* Add a pipeline to build the app from dev ? -> Staging/Beta version