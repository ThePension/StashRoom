* When opening a new repo, the state of the app should be clean/reset (close any open file in the middle panel f.e., reset the lists)
* Sometimes staging a hunk does not work
* When discarding the changes on a file (whole file at once), the file is still present on the left panel
* When creating a branch from cli, the UI is not updated (no watching mechanisms)
* Clean documentation
* If the name of a file is too long (path + name), the UI is a bit fucked
* If a file is new, we should not be able to stage a hunk
* Remove .vscode folder